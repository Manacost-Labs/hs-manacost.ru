# Shared PHP84 + legacy login PHP81: recover abnormal exits, not manual stops.
# Install as a drop-in, then daemon-reload; do not restart running PHP pools.
[Unit]
StartLimitIntervalSec=300
StartLimitBurst=5

[Service]
Restart=on-failure
RestartSec=5s
